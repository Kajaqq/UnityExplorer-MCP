# UnityExplorer Tests

Tests for the MCP surface live under this folder. Current suites:

- `dotnet/` — xUnit contract tests for the in‑process MCP server (`UnityExplorer.Mcp.ContractTests`). 

Run the .NET suite:

```bash
cd UnityExplorer-MCP/tests/dotnet
dotnet test
```
